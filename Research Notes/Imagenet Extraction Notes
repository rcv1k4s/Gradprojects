for a in *.tar
do
    a_dir=`expr $a : '\(.*\).tar'`
    mkdir $a_dir 2>/dev/null
    tar -xvzf $a -C $a_dir
    rm -r $a
done
On Docker:

Path for Train Data::/home/rchamart/imagenet/train


Path to validation Data::/home/rchamart/imagenet/val



